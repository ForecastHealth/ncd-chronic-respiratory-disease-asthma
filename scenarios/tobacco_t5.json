{
  "metadata": {
    "label": "Tobacco - T5 - Mass media campaigns",
    "description": "Mass media campaign intervention that implements effective campaigns educating the public about the harms of smoking/tobacco use and second-hand smoke.\nThis scenario exposes the parameters for public education campaigns that work by increasing awareness of tobacco health risks and motivating behavior change.\nThe intervention reduces tobacco prevalence in the population through sustained messaging about health consequences, which in turn reduces the incidence rates of asthma among susceptible individuals.\nMass media campaigns reach broad audiences with anti-tobacco messaging, counter tobacco industry marketing, and provide social support for quit attempts.\nEffective campaigns use multiple media channels and evidence-based messaging to change social norms around tobacco use and encourage cessation.\nNote: For a comprehensive understanding of tobacco control impacts, this intervention should also be run for COPD, Diabetes, and Cardiovascular Disease models.\n",
    "authors": [],
    "date_created": "2025-08-11 16:56"
  },
  "parameters": {
    "Country": {
      "hidden": true,
      "value": "AFG",
      "paths": [
        "$.nodes[?(@.id=='AsthmaEpsd_CFR')].generate_array.parameters.country",
        "$.nodes[?(@.id=='Healthy_Disability')].generate_array.parameters.country",
        "$.nodes[?(@.id=='Asthma_Disability')].generate_array.parameters.country",
        "$.nodes[?(@.id=='DsFreeSus')].generate_array.parameters.country",
        "$.nodes[?(@.id=='rXFK8r54')].generate_array.parameters.country",
        "$.nodes[?(@.id=='HvEhBiqV')].generate_array.parameters.country",
        "$.nodes[?(@.id=='wdoGuxrz')].generate_array.parameters.country",
        "$.links[?(@.id=='avvOYu19')].generate_array.parameters.country",
        "$.links[?(@.id=='MCWFcR4e')].generate_array.parameters.country",
        "$.links[?(@.id=='EXGS1xV6')].generate_array.parameters.country",
        "$.links[?(@.id=='Yfenedx0')].generate_array.parameters.country",
        "$.links[?(@.id=='09Q1rhSn')].generate_array.parameters.country",
        "$.links[?(@.id=='Z6UVoLxv')].generate_array.parameters.country",
        "$.links[?(@.id=='tGFmOvlz')].generate_array.parameters.country",
        "$.links[?(@.id=='yCtPKnQ5')].generate_array.parameters.country",
        "$.links[?(@.id=='kdE7JaZO')].generate_array.parameters.country",
        "$.links[?(@.id=='z2MaEXxg')].generate_array.parameters.country"
      ]
    },
    "Baseline Coverage Tobacco Massmedia": {
      "value": 0.05,
      "description": "The baseline coverage for the intervention",
      "paths": [
        "$.nodes[?(@.id=='kH5YhKYM')].generate_array.parameters.value",
        "$.nodes[?(@.id=='lFeWKUSt')].generate_array.parameters.start_value",
        "$.nodes[?(@.id=='lFeWKUSt')].generate_array.parameters.fill_before"
      ],
      "category": "Coverage"
    },
    "Target Coverage Tobacco Massmedia": {
      "value": 0.05,
      "description": "The target coverage for the intervention",
      "paths": [
        "$.nodes[?(@.id=='lFeWKUSt')].generate_array.parameters.end_value",
        "$.nodes[?(@.id=='lFeWKUSt')].generate_array.parameters.fill_after"
      ],
      "category": "Coverage"
    },
    "Scale-Up Start Year Tobacco Massmedia": {
      "value": 1,
      "description": "The index when scale-up starts for this intervention, where 0 == the first year of the time horizon",
      "paths": [
        "$.nodes[?(@.id=='lFeWKUSt')].generate_array.parameters.start_index"
      ],
      "category": "Coverage"
    },
    "Scale-Up Final Year Tobacco Massmedia": {
      "value": 2,
      "description": "The index when scale-up ends for this intervention, where 0 == the first year of the time horizon",
      "paths": [
        "$.nodes[?(@.id=='lFeWKUSt')].generate_array.parameters.end_index"
      ],
      "category": "Coverage"
    }
  }
}